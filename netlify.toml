[build]
  base = 'client'
  publish = '../dist/public'
  command = 'pnpm install && pnpm build'

[build.environment]
  NODE_VERSION = "22.13.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
